# WePlaning 2.3 (legacy)

Protocol 3.0 replaced the session tree. See `weplaning-v3.0-protocol.md`.

A 2.2/2.3 `.agent-memory` still **checks** until the next write. `THREADS.md` and `sessions/` are not required and are not accepted truth. Do not create new sessions.

interface:
  display_name: "WePlaning"
  short_description: "Maintain lightweight v2.3 project memory."
  default_prompt: "Use $we-planing only when project memory in .agent-memory needs initialization, durable updates, closeout, repair, or verification."

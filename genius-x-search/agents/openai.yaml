interface:
  display_name: "Genius X Search"
  short_description: "Search X via Grok-compatible x_search"
  default_prompt: "Use $genius-x-search to search X for $input"

policy:
  allow_implicit_invocation: true
